# FitLife Server

Next.js App Router backend API for FitLife.

Deployment, environment variables, migrations, seed commands, and smoke tests are documented in the root [`DEPLOYMENT.md`](../DEPLOYMENT.md).

Use `server/.env.example` as the local/production environment template.
